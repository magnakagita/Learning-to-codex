# Vault設計レビュー（スクリーンショット前提・iCloud実体非アクセス）

## 前提とレビュー方針
- **前提**: iCloud Drive / iCloudコンテナの実体（ファイルシステム・API）にはアクセスしない。
- **入力**: ユーザーが提示するスクリーンショットのみ（UI状態・設定値・ファイル構造の見た目）。
- **優先順位**: スクリプト追加より、**運用設計（ルール・手順・責任分界・監査）**の具体化を優先する。

---

## 1. 現時点のレビュー対象（スクリーンショットで確認すべき項目）
スクリーンショットベースでは、技術的真偽を100%断定できないため、以下を「見た目上の確認」と「運用ルールで担保」に分離する。

### 1-1. 見た目上の確認（スクリーンショット）
1. Vaultルート直下の主要ディレクトリ構成（例: `00_Inbox`, `10_Projects`, `20_Areas`, `30_Resources`, `90_Archive`）
2. 命名規則（英数/日本語、日付フォーマット、プロジェクトID）
3. テンプレート運用の痕跡（同一ヘッダ、タグ、メタデータの統一）
4. 参照リンク構造（MOC/インデックスページの存在）
5. 秘匿情報の扱い（平文APIキーや個人情報が画面に露出していないか）

### 1-2. 運用ルールで担保すべき点（実体アクセスなしで補う）
1. バックアップ成功可否の事後確認手順
2. 競合・上書き発生時の復旧手順
3. 変更管理（誰が・いつ・何を変更したか）の記録
4. 監査ログ（週次/月次レビュー結果）
5. 障害時エスカレーション

---

## 2. 運用設計（提案）

### 2-1. 運用責任分界（RACI簡易）
- **Owner（A）**: Vault運用責任者（1名）
  - 命名規則・フォルダ規約の最終決裁
  - 週次レビュー承認
- **Maintainer（R）**: 実運用担当（1〜2名）
  - 受信箱整理、リンク修復、アーカイブ移行
  - レビュー用スクリーンショットの作成
- **Reviewer（C）**: 設計レビュー担当（外部/AI含む）
  - スクリーンショットに基づく構造・規約準拠チェック
- **Stakeholder（I）**: 利用者
  - 変更通知の受領

### 2-2. 変更管理プロセス（軽量）
1. 変更要求を1行で記録（例: `CR-2026-04-27-01`）
2. 変更前スクショ（対象フォルダ/ノート）を保存
3. 変更実施
4. 変更後スクショを保存
5. 「差分」「理由」「影響範囲」を3点だけ記録
6. 週次レビューで承認/差し戻し

> 重要: 実体に直接アクセスできないため、**Before/Afterスクショ**を監査証跡として扱う。

### 2-3. 定常オペレーション（頻度別）

#### 日次（5〜10分）
- `00_Inbox` を空に近づける（分類・タグ付け）
- 今日変更したノートのリンク切れ目視チェック
- 競合ファイル（`conflict`, `duplicated` 等）命名の有無を確認

#### 週次（20〜30分）
- MOCのリンク到達性確認（上位10リンク）
- `10_Projects` のステータス更新（Active/On-hold/Done）
- アーカイブ候補の選定
- レビュー用スクショセット作成（固定撮影ポイント）

#### 月次（45〜60分）
- フォルダ肥大化確認（閾値超過時に分割）
- 命名規則逸脱の是正
- テンプレート改訂の要否判定
- 運用KPIレビュー

---

## 3. スクリーンショット取得標準（レビュー継続用）
レビュー品質を安定させるため、毎回同じ観点で撮る。

### 3-1. 必須ショット
1. Vaultルート全体（1枚）
2. `00_Inbox` 一覧（1枚）
3. `10_Projects` 一覧 + 任意1プロジェクト詳細（2枚）
4. MOCトップ（1枚）
5. 設定画面（同期・プラグイン関連、1〜2枚）

### 3-2. 撮影ルール
- 画面解像度・ズーム率を固定
- 個人情報/秘密情報はマスクしてから共有
- ファイル名省略表示が起きない幅で撮る
- 撮影日時（UTC/JST）をメモに添付

---

## 4. リスクと対策（iCloud非アクセス前提）

### リスクA: 同期失敗を見逃す
- **対策**: 週次で「最終更新日時の偏り」をスクショ比較。
- **運用ルール**: 2端末以上で同一ノートの反映時刻を確認。

### リスクB: 重複ノート増殖
- **対策**: 命名規則に一意IDを付与（例: `PJ-YYYY-NNN`）。
- **運用ルール**: 週次で重複疑いリストを手動レビュー。

### リスクC: 参照リンク崩壊
- **対策**: MOC中心運用 + 週次リンク点検。
- **運用ルール**: プロジェクト完了時にリンク棚卸しを必須化。

### リスクD: 監査不能
- **対策**: 変更前後スクショ + 変更ID記録。
- **運用ルール**: 監査ログを月次で1ページに集約。

---

## 5. KPI（運用品質の定量指標）
- Inbox残件数（目標: 週末時点で10件以下）
- 命名規則違反件数（目標: 月次0件）
- リンク切れ件数（目標: 週次0〜2件以内）
- アーカイブ遅延件数（目標: 完了後14日以内）
- 監査ログ欠損率（目標: 0%）

---

## 6. 次回レビューに向けた依頼フォーマット
次回は以下を共有いただくと、実体アクセスなしでも設計レビュー精度を上げられる。

1. 必須ショット（3章）
2. 直近1週間の変更ID一覧（箇条書き）
3. 困っている運用課題トップ3（優先度付き）

---

## 7. 結論
- スクリプト追加を急ぐより、まずは**運用証跡（Before/Afterスクショ + 変更ID）**を標準化するのが効果的。
- iCloud実体にアクセスできない制約下でも、撮影標準・週次レビュー・KPIを回すことで、Vaultの健全性を継続的に改善できる。
